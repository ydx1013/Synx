name: Release Obsidian Plugin

on:
  push:
    branches: [main]
    tags:
      - "v*"
      - "[0-9]*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 手动打 tag（v* 或 0.1.0）时用 tag 版本；
      # push main 时自动读取最新 tag，patch 版本 +1 并打新 tag（GITHUB_TOKEN 推送的 tag 不会递归触发本工作流）。
      - name: Compute release version
        id: version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "release_tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
            echo "trigger=tag" >> "$GITHUB_OUTPUT"
          else
            git fetch --tags --force
            LATEST=$(git tag --sort=-v:refname | grep -E '^(v|)[0-9]+\.[0-9]+\.[0-9]+$' | head -n 1)
            if [[ -z "$LATEST" ]]; then
              NEW_TAG="v0.1.1"
            else
              V="${LATEST#v}"
              IFS='.' read -r MAJ MIN PATCH <<< "$V"
              NEW_TAG="v${MAJ}.${MIN}.$((PATCH+1))"
            fi
            echo "release_tag=${NEW_TAG}" >> "$GITHUB_OUTPUT"
            echo "version=${NEW_TAG#v}" >> "$GITHUB_OUTPUT"
            echo "trigger=main" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: synx-sync/package-lock.json

      - name: Install dependencies
        working-directory: synx-sync
        run: npm ci

      - name: Type check
        working-directory: synx-sync
        run: npm run typecheck

      - name: Unit tests
        working-directory: synx-sync
        run: npm test

      - name: Audit production dependencies
        working-directory: synx-sync
        run: npm audit --omit=dev --audit-level=high

      - name: Build all workspaces
        working-directory: synx-sync
        run: |
          npm run build -w @synx/shared
          npm run build -w @synx/web
          npm run build -w @synx/worker
          npm run build -w @synx/plugin

      - name: Bump manifest version and auto tag patch release
        if: steps.version.outputs.trigger == 'main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # 质量门禁全部通过后才写回版本、提交并打 tag。
          jq --arg v "${{ steps.version.outputs.version }}" '.version = $v' \
            synx-sync/packages/plugin/manifest.json > /tmp/manifest.json
          mv /tmp/manifest.json synx-sync/packages/plugin/manifest.json
          git add synx-sync/packages/plugin/manifest.json
          git commit -m "chore(plugin): bump manifest version to ${{ steps.version.outputs.version }}"
          git push origin main
          git tag "${{ steps.version.outputs.release_tag }}"
          git push origin "${{ steps.version.outputs.release_tag }}"

      - name: Stage release files
        run: |
          mkdir -p release
          cp synx-sync/packages/plugin/main.js release/
          cp synx-sync/packages/plugin/styles.css release/
          # 将 tag 版本写入 manifest，避免 BRAT 提示版本不匹配
          jq --arg v "${{ steps.version.outputs.version }}" '.version = $v' \
            synx-sync/packages/plugin/manifest.json > release/manifest.json
          echo "--- manifest.json ---"
          cat release/manifest.json
          (cd release && zip "synx-sync-${{ steps.version.outputs.version }}.zip" manifest.json main.js styles.css)
          ls -la release/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.release_tag }}
          files: |
            release/manifest.json
            release/main.js
            release/styles.css
            release/synx-sync-${{ steps.version.outputs.version }}.zip
          generate_release_notes: true
